<!DOCTYPE html>
<html lang="hu">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= suti.nev %> - Cukrászda</title>
    <link rel="stylesheet" href="/assets/css/main.css" />
    <link rel="stylesheet" href="/css/custom.css" />
  </head>
  <body class="is-preload">
    <%- include('../partials/header') %>

    <div id="wrapper" class="divided">
      <section class="wrapper style1 align-center">
        <div class="inner">
          <h1><%= suti.nev %></h1>

          <div class="box" style="max-width: 800px; margin: 2rem auto">
            <div style="text-align: center; margin-bottom: 2rem">
              <span style="font-size: 6rem">🧁</span>
            </div>

            <dl class="style2">
              <dt><strong>Típus</strong></dt>
              <dd><%= suti.tipus %></dd>

              <dt><strong>Díjazás</strong></dt>
              <dd>
                <% if(suti.dijazott) { %>
                <span style="color: #f56a6a; font-weight: bold"
                  >⭐ Magyarország Tortája verseny díjazottja</span
                >
                <% } else { %> Nem díjazott <% } %>
              </dd>

              <% if(suti.tartalmak && suti.tartalmak.length > 0) { %>
              <dt><strong>Összetevők</strong></dt>
              <dd>
                <% suti.tartalmak.forEach(t => { %>
                <span
                  style="
                    background: #e8f5e9;
                    color: #2e7d32;
                    padding: 0.3em 0.8em;
                    border-radius: 5px;
                    display: inline-block;
                    margin: 0.2em;
                  "
                >
                  <% if(t.mentes == 'G') { %> 🌾 Gluténmentes <% } else
                  if(t.mentes == 'L') { %> 🥛 Laktózmentes <% } else if(t.mentes
                  == 'C') { %> 🍬 Cukormentes <% } else { %> <%= t.mentes %> <%
                  } %>
                </span>
                <% }) %>
              </dd>
              <% } %> <% if(suti.arak && suti.arak.length > 0) { %>
              <dt><strong>Árak</strong></dt>
              <dd>
                <table style="width: 100%; margin-top: 1em">
                  <thead>
                    <tr style="border-bottom: 2px solid #ddd">
                      <th style="text-align: left; padding: 0.5em">Egység</th>
                      <th style="text-align: right; padding: 0.5em">Ár</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% suti.arak.forEach(ar => { %>
                    <tr style="border-bottom: 1px solid #eee">
                      <td style="padding: 0.5em"><%= ar.egyseg %></td>
                      <td
                        style="
                          text-align: right;
                          padding: 0.5em;
                          font-weight: bold;
                          color: #f56a6a;
                        "
                      >
                        <%= ar.ertek.toLocaleString('hu-HU') %> Ft
                      </td>
                    </tr>
                    <% }) %>
                  </tbody>
                </table>
              </dd>
              <% } %>
            </dl>

            <hr />

            <ul class="actions stacked">
              <li><a href="/sutik" class="button">← Vissza a listához</a></li>
            </ul>
          </div>
        </div>
      </section>

      <%- include('../partials/footer') %>
    </div>

    <script src="/assets/js/jquery.min.js"></script>
    <script src="/assets/js/main.js"></script>
  </body>
</html>
